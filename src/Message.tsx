// Passcal Casing
function Message() {
  // babeljs.io
  // JSX: Javascript XML
  // TSX: Typescript XML
  const name = "anhht";
  if (name) return <h1>Hello {name}</h1>;
  else return <h1>Hello World</h1>;
}

export default Message;
